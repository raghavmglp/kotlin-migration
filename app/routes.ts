import { type RouteConfig, index } from "@react-router/dev/routes";

export default [
  // This points directly to the new file you just made
  index("routes/_index/route.tsx"),
] satisfies RouteConfig;